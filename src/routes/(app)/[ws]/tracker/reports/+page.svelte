<script lang="ts">
import ReportsPage from '$lib/modules/tracker/ReportsPage.svelte'

/**
 * `/:workspace/tracker/reports` — how the work is going, rather than what it is.
 *
 * As thin as the issues route, and for the same reason: the tracker owns its screens.
 */
</script>

<ReportsPage />
