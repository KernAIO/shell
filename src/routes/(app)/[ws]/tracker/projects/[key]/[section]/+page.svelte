<script lang="ts">
import ProjectPage from '$lib/modules/tracker/ProjectPage.svelte'

/**
 * `/:workspace/tracker/projects/:key/:section` — one project's components, milestones, cycles or
 * templates. Thin, like every other route here: the screen lives in `$lib/modules/tracker`.
 */
</script>

<ProjectPage />
