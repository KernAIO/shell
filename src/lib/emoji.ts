/**
 * The emoji catalogue moved into `@kernhq/ui`, with the picker that renders it — chat is not the
 * only module that reacts to things.
 */
export { EMOJI_GROUPS, QUICK_REACTIONS, searchEmoji } from '@kernhq/ui'
